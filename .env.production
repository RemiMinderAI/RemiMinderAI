# Production build (including Vercel): CRA treats ESLint warnings as errors when CI=true.
# This allows the app to ship while legacy unused-vars / a11y warnings are cleaned up incrementally.
DISABLE_ESLINT_PLUGIN=true
